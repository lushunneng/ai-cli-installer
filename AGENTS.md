# 仓库协作记忆

## Git 与 GitHub 操作记录

凡是在本仓库执行 Git 或 GitHub 操作，都要在仓库根目录的 `git和github记录.md` 追加一条记录。记录至少包含：日期、目的、执行的命令或操作、结果，以及必要的后续说明。先完成操作，再把实际结果写入记录；不要记录未执行的命令。

记录范围包括但不限于：查看状态、分支与日志，提交、合并、变基、标签、推送、拉取、远端配置，以及使用 `gh` 或 GitHub 网页/API 完成的操作。即使操作失败，也要记录失败原因。

每次开始 Git/GitHub 工作时，先阅读 `git和github记录.md` 了解上下文；每次完成一组相关操作后立即追加记录，并在最终回复中概括本次新增内容。
